# Editor

This folder contains editor-only code for EditorMCP.

## Assembly Definition

The `DatumStudios.EditorMCP.asmdef` assembly definition ensures this code only compiles in the Unity Editor and does not affect runtime builds.

## Usage

Editor code in this package is automatically available when the package is installed. No additional setup is required.

